--- lib/strs.c.orig	2016-10-11 00:02:34.000000000 -0400
+++ lib/strs.c	2016-10-11 00:04:51.000000000 -0400
@@ -16,6 +16,13 @@
 RCSID("$HeadURL$ $Id$");
 
 /*
+ * memmove for strings.
+ */
+char *strmove(char *dest, char *src) {
+  return memmove(dest, src, strlen(src)+1);
+}
+
+/*
  * Trim whitespace off both ends of a string.
  */
 char *strtrim(char *save)
